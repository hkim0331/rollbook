#lang racket

(provide *debug*
         *user*
         *password*
         *database*
         *server*
)

(define *debug* #f)

(define *user* "rollbook")
(define *password* "secret")
(define *database* "admin")
(define *server* "vm2017.local")
